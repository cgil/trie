#!/usr/bin/env python
from setuptools import setup, find_packages

__version__ = '0.1'

setup(
    name='trie',
    description='Simple mobile stores.',
    version=__version__,
    packages=find_packages(),
)
